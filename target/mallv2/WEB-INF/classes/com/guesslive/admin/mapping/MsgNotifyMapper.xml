<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guesslive.admin.dao.MsgNotifyMapper">

      <select id="searchList" resultType="MsgNotify">
          select * from t_msg_notify 
      </select>
      
      <update id="updateId" parameterType="MsgNotify">
         	update t_msg_notify set enable=#{enable} WHERE id=#{id}
      </update>
      
      <update id="updateIdAll"  parameterType="MsgNotify">
      		update t_msg_notify set enable=#{enable}
      </update>
      
      <select id="selectById" resultType="MsgNotify">
   		 SELECT `enable` FROM t_msg_notify WHERE id=#{id}
      </select>
</mapper>