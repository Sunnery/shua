<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guesslive.admin.dao.FeedbackMapper">

	<select id="searchFeedbackCount" resultType="int">
		select COUNT(*) from
		t_feedback f ,t_account a where f.aid = a.aid
	</select>

	<select id="searchFeedbackList" resultType="Feedback">
		select
		a.nickname,f.content,f.contact,f.createtime from t_feedback f,
		t_account a where f.aid = a.aid order by f.createtime DESC limit
		#{start},#{rows}
	</select>

</mapper>