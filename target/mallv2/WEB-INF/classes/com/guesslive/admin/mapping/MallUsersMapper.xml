<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guesslive.admin.dao.MallUsersMapper">
     
     <select id="searchMallUsersCount" resultType="int">
		select COUNT(*) from t_account
			<where>
				1=1
				<if test="nickname != null and nickname != ''">
					and nickname like concat('%',#{nickname},'%')
				</if>
				<if test="phone != null and phone != ''">
					and phone = #{phone}
				</if>
			</where>
     </select>
     
     <select id="getMallUsersList" resultType="Account">
		select aid,account,phone,nickname,sex,birthday,icon,createtime,regplatform from t_account
			<where>
				1=1
				<if test="account.nickname != null and account.nickname != ''">
					and nickname like concat('%',#{account.nickname},'%')
				</if>
				<if test="account.phone != null and account.phone != ''">
					and phone = #{account.phone}
				</if>
			</where>
		 		order by createtime DESC limit #{start},#{rows}
     </select>
          
</mapper>